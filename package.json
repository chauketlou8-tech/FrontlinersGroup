{
  "name": "frontliners-group-website",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" && \"npm run dev --prefix frontend\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "admin": "npm run dev --prefix admin",
    "all": "concurrently \"npm run dev --prefix backend\" && \"npm run dev --prefix frontend\" && \"npm run dev --prefix admin\""
  },
  "dependencies": {
    "concurrently": "^9.2.3"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
